# EmailJS Configuration# Environment Variables Setup

# Sign up at https://www.emailjs.com/ and get your credentials

# Then replace the values below with your actual credentials## 📋 Overview



REACT_APP_EMAILJS_SERVICE_ID=YOUR_SERVICE_IDThis project uses environment variables to securely store sensitive configuration like EmailJS credentials.

REACT_APP_EMAILJS_TEMPLATE_ID=YOUR_TEMPLATE_ID

REACT_APP_EMAILJS_PUBLIC_KEY=YOUR_PUBLIC_KEY## 🔧 Setup Instructions



# Backend API URL### 1. Create Your Local Environment File

# For local development

REACT_APP_API_URL=http://localhost:5000/apiCopy the `.env` template to create your local configuration:



# For production (uncomment and update the line below)```bash

# REACT_APP_API_URL=https://rubyet.info/backend/api# The .env.local file is already created for you

# Just add your actual credentials
```

### 2. Add Your EmailJS Credentials

Open `.env.local` and replace the placeholder values:

```env
REACT_APP_EMAILJS_SERVICE_ID=your_actual_service_id
REACT_APP_EMAILJS_TEMPLATE_ID=your_actual_template_id
REACT_APP_EMAILJS_PUBLIC_KEY=your_actual_public_key
```

### 3. Restart Development Server

After updating `.env.local`, restart your dev server:

```bash
npm start
```

## 🔒 Security

- ✅ `.env.local` is in `.gitignore` - your secrets are safe
- ✅ Never commit `.env.local` to Git
- ✅ The `.env` file (template) can be safely committed
- ✅ All React environment variables must start with `REACT_APP_`

## 📝 Environment Variables Reference

| Variable | Description | Where to Get |
|----------|-------------|--------------|
| `REACT_APP_EMAILJS_SERVICE_ID` | EmailJS Service ID | EmailJS Dashboard > Email Services |
| `REACT_APP_EMAILJS_TEMPLATE_ID` | EmailJS Template ID | EmailJS Dashboard > Email Templates |
| `REACT_APP_EMAILJS_PUBLIC_KEY` | EmailJS Public Key | EmailJS Dashboard > Account > General |

## 🚀 Production Deployment

When deploying to production (Vercel, Netlify, etc.), add these environment variables in your hosting platform's dashboard:

1. Go to your project settings
2. Find "Environment Variables" section
3. Add the three variables listed above
4. Redeploy your application

## 📖 More Information

See `EMAILJS_SETUP.md` for complete EmailJS setup instructions.
